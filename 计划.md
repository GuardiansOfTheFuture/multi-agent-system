# AI 知识抽取模块专业设计方案

针对你的图神经网络研究图谱场景，我将这个 AI 抽取模块设计为 **"输入 - 配置 - 校验 - 导入" 四步闭环的专业级工具 **，彻底解决当前 "黑盒抽取、不可控、易产生垃圾数据" 的问题，同时保持操作的简洁性。

------

## 一、当前设计的核心问题

1. **功能过于单薄**：仅支持纯文本粘贴，无法满足学术场景下从论文、PDF、URL 抽取的核心需求
2. **完全黑盒操作**：用户无法控制抽取的实体类型、关系类型、粒度和精度
3. **缺少校验环节**：抽取结果直接导入图谱，错误数据难以清理
4. **无状态保留**：没有抽取历史、没有模板保存，重复抽取效率极低

------

## 二、整体架构设计

将弹窗重构为 **"三区域 + 双流程"** 的专业结构：

- **左侧核心操作区**：输入源 + 抽取配置
- **右侧结果预览区**：实时预览抽取的实体和关系，支持编辑
- **底部操作栏**：执行控制 + 导入选项
- **快速流程**：默认配置，一键抽取（适合普通用户）
- **专业流程**：展开高级配置，精确控制（适合你这样的专业用户）

------

## 三、分模块详细设计

### 1. 顶部标题与状态区

```
AI知识抽取 [通义千问3.6-plus]
[保存为模板] [加载历史] [帮助]
```

- 明确显示当前使用的大模型，支持下拉切换不同模型
- 增加 "保存为模板" 按钮，可将当前的抽取配置保存为模板（如 "论文摘要抽取模板"、"实验结果抽取模板"）
- 增加 "抽取历史"，可查看和复用过去 7 天的抽取记录

### 2. 输入源模块（核心增强）

**替换单一的文本框为多输入源标签页**：

- **📝 文本粘贴**（默认）：保留大文本框，增加 "清空"、"字数统计"、"自动分段" 按钮
- **📄 文件上传**：支持上传 PDF、Word、TXT、Markdown 文件，自动提取文本内容
- **🔗 URL 导入**：输入论文链接（ArXiv、IEEE Xplore、CNKI 等），自动爬取摘要和全文
- **📚 文献库选择**：从系统已有的文献库中直接选择论文进行抽取

### 3. 抽取配置模块（专业度核心）

使用**折叠面板**组织，默认只显示核心配置，高级配置可展开：

#### 核心配置（默认展开）

| 配置项       | 控件类型 | 默认值           | 说明                                                         |
| :----------- | :------- | :--------------- | :----------------------------------------------------------- |
| 抽取主题     | 输入框   | 自动从文本中提取 | 可选，用于引导 AI 聚焦特定主题                               |
| 抽取实体类型 | 多选框组 | 全选             | 与左侧筛选面板的实体类型完全一致（概念、论文、作者、方法、数据集、主题、问题、发现） |
| 抽取关系类型 | 多选框组 | 全选             | 与左侧筛选面板的关系类型完全一致（使用、扩展、属于、矛盾、相关、提出、评估、引用） |
| 置信度阈值   | 滑块     | 0.7              | 只保留置信度高于此值的结果，0.5 = 宽松，0.7 = 平衡，0.9 = 严格 |

#### 高级配置（默认折叠）

- 抽取粒度

  ：单选（细粒度 / 中粒度 / 粗粒度）

  - 细粒度：抽取尽可能多的细粒度实体和关系
  - 中粒度：平衡数量和质量，推荐使用
  - 粗粒度：只抽取核心的、重要的实体和关系

- 去重策略

  ：单选（自动合并 / 保留全部 / 提示用户）

  - 自动合并：自动合并名称相同或相似的实体
  - 保留全部：不进行任何去重
  - 提示用户：抽取完成后提示用户手动合并

- 自定义提示词

  ：文本域

  - 允许用户覆盖系统默认的提示词，实现完全自定义的抽取逻辑

- 是否抽取元数据

  ：开关（默认开启）

  - 自动抽取论文的标题、作者、发表年份、期刊、DOI 等元数据

### 4. 执行与进度模块

点击 "抽取" 按钮后，显示**分阶段进度条**：

```
正在分析文本... 20%
正在识别实体... 50%
正在提取关系... 80%
正在生成图谱... 100%
```

- 每个阶段显示具体的进度和正在处理的内容
- 增加 "取消" 按钮，可随时中断抽取过程
- 抽取失败时显示详细的错误信息和 "重试" 按钮

### 5. 结果预览与校验模块（最关键的新增）

抽取完成后，弹窗自动切换为**结果预览模式**，这是避免错误数据进入图谱的核心环节：

- 左侧：实体列表

  - 按实体类型分组显示所有抽取到的实体
  - 每个实体显示名称、类型、置信度（用颜色区分：绿色 = 高置信度，黄色 = 中置信度，红色 = 低置信度）
  - 支持编辑实体名称、修改实体类型、删除实体、合并实体
  - 支持手动添加新实体

  

- 右侧：关系列表

  - 按关系类型分组显示所有抽取到的关系
  - 每个关系显示 "源实体 - 关系类型 - 目标实体" 和置信度
  - 支持修改关系类型、删除关系
  - 支持手动添加新关系

- 底部：图谱预览

  - 用迷你图谱的形式可视化显示抽取结果
  - 与主图谱使用完全相同的样式和交互
  - 支持拖拽、缩放，直观查看抽取效果

### 6. 导入选项与操作栏

```
[返回编辑] [重新抽取]  导入方式：○ 创建新图谱  ● 合并到当前图谱  ○ 仅保存结果
[取消] [导入图谱]
```

- **返回编辑**：回到输入和配置页面，修改后重新抽取
- **重新抽取**：使用相同的配置重新执行抽取
- 导入方式：
  - 创建新图谱：将抽取结果创建为一个新的独立图谱
  - 合并到当前图谱：将抽取结果合并到当前打开的图谱中（推荐）
  - 仅保存结果：将抽取结果保存到历史记录，不导入图谱
- **导入图谱**：执行导入操作，导入完成后自动关闭弹窗并刷新主图谱

------

## 四、视觉与交互优化

1. **保持视觉一致性**：所有颜色、图标、字体与主界面完全统一，实体和关系的颜色与左侧筛选面板一一对应
2. **分层设计**：使用卡片、阴影和分割线区分不同的功能区域，提升层次感
3. **即时反馈**：所有操作都有即时的视觉反馈，如按钮悬停效果、加载动画、成功 / 失败提示
4. 快捷键支持：
   - `Ctrl+V`：快速粘贴文本
   - `Ctrl+Enter`：执行抽取
   - `Esc`：关闭弹窗
5. **响应式设计**：弹窗支持拖拽调整大小，内容区域自动适应

------

## 五、进阶专业功能

1. **批量抽取**：支持同时上传多个文件或选择多篇论文进行批量抽取
2. **增量抽取**：对同一篇论文进行多次抽取时，只导入新增的实体和关系
3. **抽取质量评估**：自动评估本次抽取的质量，给出评分和改进建议
4. **模型对比**：支持同时使用多个模型进行抽取，对比不同模型的抽取结果
5. **导出功能**：支持将抽取结果导出为 JSON、CSV、Excel **等格式**